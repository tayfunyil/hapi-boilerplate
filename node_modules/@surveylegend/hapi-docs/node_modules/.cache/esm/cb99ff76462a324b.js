let Joi;_462‍.x([["default",()=>_462‍.o]]);_462‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);

const tags = {
    options: Joi.array().items(
        Joi.object({
            name: Joi.string().required(),
            descriptions: Joi.array(),
            order: Joi.number(),
            deprecated: Joi.boolean(),
            internal: Joi.boolean(),
            experimental: Joi.boolean(),
            uppercase: Joi.boolean()
        }).optional()
    )
}

tags.build = options => {
    let settings = []

    if (options.tags) {
        Joi.assert(settings, tags.options)
        settings = options.tags
    }

    return settings
}

_462‍.d(tags);
