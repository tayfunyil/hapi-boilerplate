let Joi;_e7f‍.x([["default",()=>_e7f‍.o]]);_e7f‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);

const errors = {
    options: Joi.array().items(
        Joi.object({
            descriptions: Joi.array(),
            codes: Joi.array().items(
                Joi.object({
                    status: Joi.string(),
                    description: Joi.string()
                })
            )
        })
    )
}

errors.build = options => {
    let settings = []

    if (options.errors) {
        Joi.assert(settings, errors.options)
        settings = options.errors
    }

    return settings
}

_e7f‍.d(errors);
