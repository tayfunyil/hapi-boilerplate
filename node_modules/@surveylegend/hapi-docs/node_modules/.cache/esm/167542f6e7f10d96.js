_e7f‍.x([["default",()=>_e7f‍.o]]);const utilities = {}

utilities.removeTrailingSlash = string => {
    if (string.endsWith('/')) {
        return string.slice(0, -1)
    }

    return string
}

utilities.slugify = value => {
    if (typeof value !== 'string') {
        return false
    }

    return value
        .toLowerCase()
        .trim()
        .replace(/[\s\W-]+/g, '_')
}

_e7f‍.d(utilities);
